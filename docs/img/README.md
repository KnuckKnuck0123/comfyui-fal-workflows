# Screenshots

Drop workshop screenshots here. The main `README.md` references:

- `hero.png` — ComfyUI with the workflows sidebar open, all 27 loaded.

Suggested extras for a good workshop feel:
- `abstract_flux_result.png` — a nice output from the starter workflow
- `render_kontext_before_after.png` — sketch → render pair
- `sidebar.png` — closeup of the Workflows sidebar showing the categories
